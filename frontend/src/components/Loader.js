import React from "react";

const Loader = () => {
  return (
    <div className="border w-auto h-auto rounded-sm">
      <span className="sr-only">Loading...</span>
    </div>
  );
};

export default Loader;
